#pins.transitions-enabled
 - @pins.each do |pin|
  .box.panel.panel-default
   = link_to (image_tag pin.image_url(:original)), pin
   .panel_body
    %h2= link_to pin.title, pin
    %p.user
    Submited by
    =pin.user.email
 